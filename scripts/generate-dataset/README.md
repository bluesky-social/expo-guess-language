# script for generating dataset

streams in the firehose, compares stated language to lingua, and outputs the ones that match to a json file

to run:

```bash
go run . -count 500 -out ../../example/assets/dataset.json
```
